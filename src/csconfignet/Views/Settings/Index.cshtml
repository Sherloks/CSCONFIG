@using Data.Models
@model IEnumerable<Data.Models.Setting>

@{
    ViewData["Title"] = "Index";
}


<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.GameDirectory)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PreferedLanguage)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PreferedTheme)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GameDirectory)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PreferedLanguage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PreferedTheme)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
